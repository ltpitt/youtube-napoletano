import os
from pathlib import Path

# Example configuration for youtube-napoletano
# Copy this file to youtube_napoletano/config.py and adjust as needed

YTDLP_PATH = os.getenv(
    "YTDLP_PATH", str(Path(__file__).parent.parent / ".venv/bin/yt-dlp")
)
PYTHON_PATH = os.getenv(
    "PYTHON_PATH", str(Path(__file__).parent.parent / ".venv/bin/python3")
)
OUTPUT_DIR = os.getenv("OUTPUT_DIR", str(Path(__file__).parent.parent / "downloads"))
UPDATE_TIMESTAMP_FILE = os.getenv(
    "UPDATE_TIMESTAMP_FILE",
    str(Path(__file__).parent.parent / "yt-dlp-last-update.txt"),
)
